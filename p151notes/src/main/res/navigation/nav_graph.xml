<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ContentFragment">
    <fragment
        android:id="@+id/NoteFragment"
        android:name="com.triare.p151notepadapp.ui.note.NoteFragment"
        android:label="fragment_note"
        tools:layout="@layout/fragment_note" />
    <fragment
        android:id="@+id/ContentFragment"
        android:name="com.triare.p151notes.ui.content.ContentFragment"
        android:label="fragment_content"
        tools:layout="@layout/fragment_content">
        <action
            android:id="@+id/action_contentFragment_to_noteFragment"
            app:destination="@id/NoteFragment" />
    </fragment>
</navigation>